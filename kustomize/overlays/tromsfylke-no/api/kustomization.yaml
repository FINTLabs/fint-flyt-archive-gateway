apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tromsfylke-no

resources:
  - ../../../base

labels:
  - pairs:
      app.kubernetes.io/instance: fint-flyt-archive-gateway_tromsfylke_no
      fintlabs.no/org-id: tromsfylke.no

patches:
  - patch: |-
      - op: replace
        path: "/metadata/labels/app.kubernetes.io~1instance"
        value: "fint-archive-gateway_tromsfylke_no"
      - op: replace
        path: "/spec/kafka/acls/0/topic"
        value: "tromsfylke-no.flyt.*"
      - op: replace
        path: "/spec/orgId"
        value: "tromsfylke.no"
      - op: replace
        path: "/spec/url/basePath"
        value: "/tromsfylke-no"
      - op: replace
        path: "/spec/observability/metrics/path"
        value: "/tromsfylke-no/actuator/prometheus"
      - op: replace
        path: "/spec/ingress/basePath"
        value: "/tromsfylke-no/api/intern/arkiv"
      - op: add
        path: "/spec/env/-"
        value:
          name: "novari.flyt.archive.gateway.client.fint-archive.base-url"
          value: "https://api.felleskomponent.no"
      - op: add
        path: "/spec/env/-"
        value:
          name: "novari.flyt.resource-server.security.api.internal.authorized-org-id-role-pairs-json"
          value: |
            {
              "tromsfylke.no":["USER"],
              "vigo.no":["DEVELOPER", "USER"],
              "novari.no":["DEVELOPER", "USER"]
            }
      - op: add
        path: "/spec/env/-"
        value:
          name: "novari.kafka.topic.org-id"
          value: "tromsfylke-no"
      - op: replace
        path: "/spec/onePassword/itemPath"
        value: "vaults/aks-api-vault/items/fint-flyt-v1-slack-webhook"
      - op: replace
        path: "/spec/probes/readiness/path"
        value: "/tromsfylke-no/actuator/health"
      - op: replace
        path: "/spec/observability/metrics/path"
        value: "/tromsfylke-no/actuator/prometheus"
    target:
      kind: Application
      name: fint-flyt-archive-gateway

  - patch: |-
      - op: replace
        path: "/metadata/labels/app.kubernetes.io~1instance"
        value: "fint-flyt-archive-fint-client_tromsfylke_no"
      - op: replace
        path: "/spec/orgId"
        value: "tromsfylke.no"
    target:
      kind: FintClient
      name: fint-flyt-archive-fint-client
